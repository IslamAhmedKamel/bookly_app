import 'package:flutter/material.dart';

class DatailsViewBody extends StatelessWidget {
  const DatailsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}